#ifndef PROJECT_EULER_SOLUTIONS_H
#define PROJECT_EULER_SOLUTIONS_H

#include <cstddef>

size_t multiples_of_3_and_5(size_t);

size_t arithmetic_progression_sum(size_t, size_t, size_t);

size_t even_fibonacci_numbers(size_t);

long long largest_prime_factor(long long);

#endif
